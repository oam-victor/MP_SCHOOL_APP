version: '3'
services:
  school-front:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:5173"
    image: odamvictor/school-front:latest
  school-back:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    image: odamvictor/school-back:latest
